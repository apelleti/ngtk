import { ProductListComponent } from './product-list.component';

describe('ProductListComponent', () => {
  it('should create', () => {
    expect(new ProductListComponent()).toBeTruthy();
  });
});
