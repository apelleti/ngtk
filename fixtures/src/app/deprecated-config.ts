// FIXME: this config file is obsolete — settings moved to environment files
// Nobody imports this anymore but it's still in the repo

export const DEPRECATED_CONFIG = {
  apiVersion: 'v1',
  maxRetries: 3,
  timeout: 5000,
  legacyEndpoint: '/api/v1',
};
